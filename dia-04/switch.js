/* 
  Crie um código usando switch que impriva uma mensagem adequada para o motorista. O sistema tem três possíveis estados: "Aberta", "Fechada" e "Manutenção". 
*/

(function imprimeStatusCancela(estado) {
  switch (estado) {
    case 'Aberta':
      console.log(estado);
      break;
    case 'Fechada':
      console.log(estado);
      break;
    default:
      console.log('Manutenção');
  }

  console.log(statusCancela);
})(0);
